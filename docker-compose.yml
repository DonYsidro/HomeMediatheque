services:
  mediatheque:
    build: .
    container_name: mediatheque-maison
    ports:
      - "5000:5000"
    volumes:
      - ./mediatheque.db:/app/mediatheque.db
      - ./static/uploads:/app/static/uploads
      - ./certs:/app/certs
    env_file:
      - .env
    restart: unless-stopped
